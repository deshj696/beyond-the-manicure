# Replaceable Image Assets

Place your images in this folder using these filenames:

- `hero-nail-illustration.png` for the uploaded nail illustration in the hero section.
- `final-nail-set.png` for the final five-nail gallery image.

You can also change the `src` values in `index.html` if you prefer different filenames.
